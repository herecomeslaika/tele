# 07 — AI 辅助编程反思

## 1. 核心 Prompt 摘要

### 1.1 第一轮：工程骨架搭建
- **Prompt 核心指令**：[填写：例如"生成基础目录结构、模块职责定义、配置样例和最小启动入口代码"]
- **关键约束**：[填写：例如"绝对不要一次性生成完整的协议实现"]

### 1.2 第二轮：协议核心校验与状态机
- **Prompt 核心指令**：[填写：例如"实现核心的校验组件和状态流转代码"]
- **关键约束**：[填写：例如"绝对不要在这一步实现具体的大模型网络调用逻辑"]

### 1.3 第三轮：Provider 适配层与可观测性
- **Prompt 核心指令**：[填写：例如"实现 Provider 适配层和结构化可观测性体系"]
- **关键约束**：[填写：例如"只进行调用和适配，不修改已有核心逻辑"]

### 1.4 第四轮：扩展目标与最终收尾
- **Prompt 核心指令**：[填写：例如"实现多 Provider 路由隔离和 OpenTelemetry 追踪"]
- **关键约束**：[填写：例如"不破坏现有主链路代码"]

---

## 2. AI 生成代码的人工修改点

### 2.1 修改列表
| 文件 | 修改内容 | 修改原因 |
|------|----------|----------|
| [填写] | [填写] | [填写] |

### 2.2 修改模式分析
- [填写：例如"大部分修改是配置参数微调，核心逻辑无需改动" 或 "状态机转换表需要手动补充一条边界路径"]

---

## 3. 验证方式

### 3.1 自动化测试
- 测试框架：pytest + pytest-asyncio
- 总用例数：91
- 通过率：100%
- 命令：`python -m pytest tests/ -v`

### 3.2 证据收集
- 证据脚本：`scripts/collect_evidence.py`
- 输出位置：`evidence/extension-goals/`
- 命令：`python scripts/collect_evidence.py`

### 3.3 手动验证项
- [填写：例如"使用 RealProviderAdapter 对 DeepSeek 发起真实流式调用，确认 STREAM_CHUNK 逐 token 产出"]

---

## 4. 经验总结

### 4.1 Prompt 设计经验
- [填写：例如"分轮次、明确约束边界'绝对不要做X'比'请做Y'更有效"]

### 4.2 AI 代码审查要点
- [填写：例如"AI 生成的异常处理容易遗漏边界，终态拦截逻辑需要人工确认"]

### 4.3 改进建议
- [填写：例如"下一轮可以要求 AI 在每个模块开头用 docstring 声明不变量"]
